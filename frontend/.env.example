# RotoMart frontend env — copy to .env for local overrides (optional).
# Leave UNSET for local dev (Vite proxies /api to localhost:4000).
# In production (Vercel), set this to the deployed backend origin, no trailing slash:
#   VITE_API_BASE_URL=https://rotomart-api.onrender.com
VITE_API_BASE_URL=
